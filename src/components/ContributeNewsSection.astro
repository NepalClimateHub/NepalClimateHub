---
import contributeImg from '../assets/contribute.png';
---
<section class="contribute-news-section">
  <div class="news-container">
    <div class="news-text">
      <h2>News</h2>
      <p>Stay informed about national and international climate developments. Share important climate news and updates to help our community stay current with the latest information.</p>
      <div class="news-why-title">Why submit news?</div>
      <ul class="news-list">
        <li><span class="arrow">&rarr;</span> Keep the community informed about important climate developments</li>
        <li><span class="arrow">&rarr;</span> Highlight local climate stories that may not get mainstream attention</li>
        <li><span class="arrow">&rarr;</span> Connect Nepal's climate movement with global climate action</li>
      </ul>
      <p class="news-note">Sharing knowledge empowers our community to take informed climate action!</p>
      <button class="news-btn">Submit News</button>
    </div>
    <div class="news-image">
      <img src={contributeImg.src || "/placeholder.svg"} alt="Climate news" />
    </div>
  </div>
</section>

<style>
.contribute-news-section {
  width: 100%;
  background: #fafbfc;
  margin-top: 3.5rem;
  padding: 3.5rem 0;
}

.news-container {
  max-width: 1280px;
  margin: 0 auto;
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 26px; /* Exact gap from Figma */
}

.news-text {
  flex: 0 0 532px; /* Exact width from Figma */
  min-width: 320px;
  padding-bottom: 24px; /* Add padding to ensure content stays within image height */
}

.news-text h2 {
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 16px; /* Reduced spacing */
  color: #1a1b1e;
}

.news-text p {
  font-size: 1.18rem;
  color: #222;
  margin-bottom: 12px; /* Reduced spacing */
  line-height: 1.3; /* Reduced line height for tighter text */
}

.news-why-title {
  font-weight: 700;
  margin-bottom: 6px; /* Reduced spacing */
  font-size: 1.08rem;
  margin-top: 16px; /* Reduced spacing */
}

.news-list {
  list-style: none;
  padding: 0;
  margin: 0 0 16px 0; /* Reduced spacing */
}

.news-list li {
  display: flex;
  align-items: flex-start;
  font-size: 1.08rem;
  margin-bottom: 6px; /* Reduced spacing */
  color: #222;
  line-height: 1.3; /* Reduced line height */
}

.arrow {
  display: inline-block;
  margin-right: 8px;
  font-size: 1.2em;
}

.news-note {
  font-size: 1.08rem;
  color: #222;
  margin-bottom: 12px; /* Reduced spacing */
  line-height: 1.3; /* Reduced line height */
}

.news-btn {
  background: #1a1b1e;
  color: #fff;
  border: none;
  border-radius: 2em;
  padding: 0.6em 1.5em;
  font-size: 1rem;
  font-weight: 500;
  cursor: pointer;
  transition: background 0.2s;
  margin-top: 4px; /* Minimal top margin */
}

.news-btn:hover {
  background: #333;
}

.news-image {
  flex: 1;
  display: flex;
  justify-content: flex-end;
}

.news-image img {
  width: 572px;
  height: 411px;
  max-width: 623px;
  max-height: 411px;
  object-fit: cover;
  border-radius: 12px;
  background: #eee;
}

/* Specific styles for 1024px screens to match Figma */
@media (max-width: 1200px) and (min-width: 1025px) {
  .news-container {
    padding: 0 42px;
    gap: 26px;
  }
  
  .news-text {
    flex: 0 0 48%;
  }
  
  .news-image {
    flex: 0 0 48%;
  }
  
  .news-image img {
    width: 100%;
    height: auto;
    aspect-ratio: 572/411;
  }
}

/* Specific styles for exactly 1024px to match Figma */
@media (width: 1024px) {
  .news-container {
    padding: 0 42px;
    flex-direction: row;
    gap: 26px;
  }
  
  .news-text {
    flex: 0 0 532px; 
  }
  
  .news-image img {
    width: 100%;
    height: auto;
    aspect-ratio: 572/411;
  }
}

/* Tablet styles */
@media (max-width: 1024px) and (min-width: 769px) {
  .news-container {
    padding: 0 42px;
    flex-direction: row;
    gap: 26px;
  }
  
  .news-text {
    flex: 1;
    min-width: 320px;
  }
  
  .news-image {
    flex: 1;
  }
  
  .news-image img {
    width: 100%;
    height: auto;
    aspect-ratio: 572/411;
  }
}

/* Mobile styles */
@media (max-width: 768px) {
  .contribute-news-section {
    padding: 2.5rem 0;
  }
  
  .news-container {
    flex-direction: column;
    align-items: flex-start;
    gap: 2rem;
    padding: 0 24px;
  }
  
  .news-text {
    flex: 0 0 100%;
    width: 100%;
    padding-bottom: 0;
  }
  
  .news-text h2 {
    font-size: 1.5rem;
    margin-bottom: 12px;
  }
  
  .news-text p, .news-list li, .news-note {
    font-size: 1rem;
  }
  
  .news-image {
    width: 100%;
    justify-content: center;
    order: -1; /* Ensure image stays at the top on mobile */
  }
  
  .news-image img {
    width: 100%;
    height: auto;
    max-width: 100%;
    aspect-ratio: 16/9;
  }
}

/* Very small mobile screens */
@media (max-width: 480px) {
  .news-container {
    padding: 0 18px;
  }
  
  .news-text h2 {
    font-size: 1.3rem;
  }
  
  .news-text p, .news-list li, .news-note {
    font-size: 0.98rem;
  }
}
</style>